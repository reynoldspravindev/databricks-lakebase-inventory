# Environment Configuration Template for Online Ordering & Click-and-Collect System
# Copy this file to .env and fill in your actual values

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
SECRET_KEY=your-secret-key-here
PORT=8080

# ============================================================================
# DATABRICKS CONFIGURATION
# ============================================================================
# Your Databricks workspace host (required for OAuth mode)
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com

# ============================================================================
# POSTGRES DATABASE CONFIGURATION
# ============================================================================
# Database connection details
PGHOST=your-postgres-host.databricks.com
PGPORT=443
PGDATABASE=your_database_name
PGSSLMODE=require
PGAPPNAME=ordering_app

# ============================================================================
# AUTHENTICATION MODE - Choose ONE of the following options:
# ============================================================================

# OPTION 1: OAuth Authentication (for Databricks Apps deployment)
# Use this when deploying as a Databricks App
# The app will automatically use OAuth tokens for authentication
PGUSER=your-oauth-user

# OPTION 2: Explicit Postgres User (for local development)
# Use this for local development or when you have dedicated postgres credentials
# Uncomment and fill these values:
# POSTGRES_USER=your_postgres_username
# POSTGRES_PASSWORD=your_postgres_password

# ============================================================================
# DEPLOYMENT INSTRUCTIONS
# ============================================================================

# FOR LOCAL DEVELOPMENT:
# 1. Create a dedicated Postgres user in Databricks
# 2. Use OPTION 2 (POSTGRES_USER and POSTGRES_PASSWORD)
# 3. Make sure PGHOST, PGPORT, PGDATABASE are set correctly
# 4. Run: python app.py

# FOR DATABRICKS APP DEPLOYMENT:
# 1. Use OPTION 1 (PGUSER with OAuth)
# 2. Deploy as a Databricks App
# 3. The app will use OAuth tokens automatically

# ============================================================================
# SCHEMA NAMING
# ============================================================================
# The app automatically creates schemas in the format:
# {PGAPPNAME}_schema_{PGUSER}
# 
# Example: ordering_app_schema_myuser
# This ensures each deployment has its own isolated schema

# ============================================================================
# SAMPLE VALUES FOR REFERENCE
# ============================================================================
# PGHOST=your-workspace-123456.sql.gcp.databricks.com
# PGDATABASE=shared_database
# PGUSER=your_username
# PGAPPNAME=ordering_app